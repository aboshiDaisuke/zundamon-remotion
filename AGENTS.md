# ずんだもん＆四国めたん 解説動画制作ルール（AGENTS.md）

このワークスペースでRemotionとVOICEVOXを用いた解説・掛け合い動画を制作・改修する際は、以下のルールを厳格に遵守してください。

---

## 0. ゼロからの超簡単導入（VOICEVOX ＆ Remotion）

1. **VOICEVOXの導入（所要1分）**:
   - [VOICEVOX公式サイト](https://voicevox.hiroshiba.jp/)から無料アプリをダウンロード・起動（Mac / Windows両対応）。
   - ポート `50021` でAPIが自動待機するため、アプリを開いておくだけでAIが自動連携・音声合成を行います。
2. **Remotionの導入（所要2分）**:
   - Node.js（v18以上）が入った環境で `npm install` を実行するだけ。
   - `npm run render` 一発で動画が書き出せます。
3. **立ち絵PSDの放り込み＆自動リップシンク（超重要）**:
   - 坂本アヒル様の「ずんだもん立ち絵素材.psd」「四国めたん立ち絵素材.psd」を**プロジェクトルートにそのまま置くだけでOK**。
   - 面倒なパーツ切り抜き作業は一切不要。スクリプトが目・口・眉・身体レイヤーを自動抽出します。
   - **自動口パク（リップシンク）**: 音声波形に合わせて自動で口が開閉。
   - **自動目パチ（まばたき）**: 自然な周期でまばたきアニメーションが動作。
   - **自動バウンド演出**: 喋り始めにピョコッとジャンプ。
   - **感情表情連動**: ドヤ顔、困り顔、笑顔、怒り顔がセリフに合わせて自動切り替え。

---

## 1. デザイン＆レイアウト設計ルール（被りゼロ・高品位維持）

1. **画面要素の絶対分離原則**:
   - **画面上部（y: 8px〜60px）**: ヘッダーバー。右端に独立した金枠（`#ffd54f`）＋深緑グラデーションの番組風コーナーテロップ（20px）を配置。中央カード内にサブタイトルを小さく押し込まないこと。
   - **画面中央（y: 70px〜680px, width: 860px）**: 中央解説カード。左右対称の中央揃えとし、上部にカテゴリバッジ（16px）、大見出し（38px）、イラスト/画像（高さ約385px）、解説ポイント（24px、丸型ナンバリングバッジ❶❷❸＋深緑バーアクセント）をゆったり配置。
   - **画面左右（x: 40px / x: 1450px）**: キャラクター配置（左: ずんだもん、右: 四国めたん）。中央カードや字幕バーと重ならない絶対安全マージンを確保。
   - **画面下部（y: 930px〜1040px）**: 字幕バー。中央カード下端から最低150px以上のスペースを空け、話者ネームプレート（ずんだもん: 緑、めたん: ピンク）を添えて32〜34pxで表示。

2. **フォント適用の絶対ルール（けいフォント完全適用）**:
   - ヘッドレスChromiumではWebフォントのロード前に描画されるとヒラギノ角ゴ等にフォールバックしてしまう。
   - **必ず `useEnsureKeiFont()`（`delayRender` ＋ `document.fonts.ready` ＋ `FontFace.load()`）を用いて待機すること**。
   - 大見出し、解説ポイント、右上テロップ、字幕のすべてに正真正銘の「けいフォント」を100%適用すること。

---

## 2. 台本・ストーリー構成ルール

1. **ユーザー提供ネタの「掛け合いコント化」**:
   - ユーザーから提供された地域ネタ、あるあるネタ、自虐ネタを「1台詞」で終わらせてはならない。
   - ずんだもんのボケ/問題提起 ➔ めたんの勘違い/ツッコミ ➔ ずんだもんの真実暴露 ➔ めたんの同調・共感 ➔ ずんだもんのオチ（コンプレックス等）というように、**最低3〜5往復のテンポ良い掛け合いコントとして深掘りすること**。

2. **話速とテンポ**:
   - VOICEVOXの基本話速は `1.15〜1.18倍`、抑揚 `1.15〜1.20` を基準とし、YouTube・動画視聴者が退屈しない軽快なテンポ感を維持する。
   - 漢字の読み間違い（例: 徳島市 ➔「トクシマ市」、汽車 ➔「きしゃ」等）は `voice_text` でひらがな/カタカナ発音を指定して防ぐ。

3. **シーン管理と連番**:
   - 掛け合いにより複数シーンに分かれる場合でも、トピック番号（`topic_num`）を保持し、画面上の表示（例: 「徳島市PR #3」）がズレないように設計する。

---

## 3. 検証・レンダリング・クリーンアップ運用ルール

1. **部分プレビュー時の明示**:
   - 特定シーンのみの短尺プレビューをレンダリングして見せる際は、「これは〇〇シーンの確認用プレビューです。全編動画は別でレンダリングします」と明確にユーザーに伝えること。ユーザーに「短くなった・他の話が消えた」と誤解させない。

2. **静止画チェックの徹底**:
   - 動画を全編レンダリングする前に、必ず該当フレームの静止画（`npx remotion still ...`）をレンダリングし、画像・文字被り・フォント・話者表情を自律確認すること。

3. **成果物ディレクトリ（`out/`）のクリーンアップ**:
   - 制作中に生成される大量の中間プレビュー画像（`preview_*.png`, `still_*.png`）やテスト音声（`test_*.wav`）、旧バージョンのmp4は定期的に整理し、ユーザーには**最新・完全版の成果物のみ**を分かりやすく提供する。

---

## 4. 必須素材・外部サービス・クレジット一覧

動画の制作・更新に必要なソフトウェア、API、アセット素材および規約は以下の通りです。

| 分類 | 名称 / サービス | 入手先 / パス / 規格 | 用途・役割 | クレジット表記規約 |
|---|---|---|---|---|
| **音声合成エンジン** | **VOICEVOX** | ローカル起動（`http://127.0.0.1:50021`）<br>公式: https://voicevox.hiroshiba.jp/ | ずんだもん（ID:3）、四国めたん（ID:2）の高品質音声合成 | 動画内または概要欄に「VOICEVOX:ずんだもん」「VOICEVOX:四国めたん」を記載 |
| **フォント** | **けいフォント** | `public/fonts/keifont.ttf`<br>配布元: フォントな（MODI工場様） | 大見出し、解説ポイント、字幕、右上テロップ等の標準デザインフォント | 商用利用可（フォント利用規約に準拠） |
| **キャラクター立ち絵** | **ずんだもん・四国めたん立ち絵素材** | ルート直下 `*.psd` / `public/characters/`<br>作者: **坂本アヒル様** | ずんだもん・めたんのポーズ差分・表情差分・口パク連動 | 「立ち絵素材: 坂本アヒル様」を明記 |
| **背景イラスト** | **みんちりえ（フリー背景素材）** | `public/backgrounds/minchirie_*.jpg`<br>配布元: https://minchirie.info/ | 街並み、駅前、リビング等の背景ビジュアル | 「背景: みんちりえ」を画面右下またはクレジットに表示 |
| **BGM** | **「2:23 AM」（しゃろう様）** | `public/audio/bgm.mp3`<br>配布元: DOVA-SYNDROME / YouTube | 軽快でテンポの良い解説用アンビエントBGM | 「BGM: しゃろう」をクレジット記載 |
| **自動化スクリプト** | **Python 3** | `scripts/generate_dual_audio.py` | VOICEVOX API連携、音声合成、JSONメタデータ・尺自動算出 | プロジェクト内保守スクリプト |

---

## 5. キャラクター変更 ＆ 素材調達・安全運用ルール

1. **キャラクター・声の自由な変更**:
   - ずんだもん・四国めたん以外にも、VOICEVOXのキャラクター（春日部つむぎ ID:8、冥鳴ひまり ID:14、玄野武宏 ID:11等）に音声IDを変更可能。
   - 立ち絵PSD素材をプロジェクトルートに配置すれば、パーツ自動分解・リップシンク・目パチが即座に動作する。
2. **フリー素材の調達と安全運用（スクレイピング制限）**:
   - ユーザーから「〇〇のフリー素材を探して」と依頼された場合、AIは商用利用可能な無料素材（みんちりえ、DOVA-SYNDROME、いらすとや等）を調査・提案する。
   - **⚠️ スクレイピング注意**: 相手先サーバーに負荷をかける過剰なクローリング・スクレイピングは固く禁止。公式に提供されているリソースや正規の配布ページから規約を守って安全に取得すること。

