# CLAUDE.md - ずんだもん＆四国めたん Remotion動画制作ガイドライン

このプロジェクトは、Remotion（React 18 + TypeScript）とVOICEVOXを用いた解説・掛け合い動画を制作するワークスペースです。
macOS / Windows のどちらでも動作するよう設計されています。

---

## 1. 開発・実行コマンド（OS共通）

すべてのコマンドは `npm run` 経由で実行でき、macOS（zsh/bash）および Windows（PowerShell/CMD）の両方に対応しています。

- **音声合成 ＆ 尺自動計算**: `npm run audio:dual`
  - VOICEVOX（ポート50021）を呼び出し、WAV生成と `src/dual_scene_data.json` のフレーム数を自動更新。
- **静止画チェック（事前確認）**: `npm run still`
  - 1フレームのみ画像出力し、文字被りやフォント、表情を目視点検。
- **フル動画レンダリング**: `npm run render`
  - `out/zundamon_metan.mp4` に本編を書き出し。
- **成果物クリーンアップ**: `npm run clean`
  - 最新の `zundamon_metan.mp4` 以外の中間プレビューファイルを安全に一括削除。
- **プレビューサーバー起動**: `npm start`
- **解説ウェブサイト起動**: `npm run serve:web`

---

## 2. 厳格なレイアウト＆デザイン原則（被りゼロ）

1. **要素の絶対分離**:
   - **画面最上部（y: 8〜60px）**: ヘッダー。右端に独立した金枠（`#ffd54f`）＋深緑グラデーションのコーナーテロップ（20px けいフォント）。中央カード内にサブタイトルを小さく押し込まないこと。
   - **画面中央（y: 70〜680px, width: 860px）**: 中央解説カード。中央揃えでカテゴリバッジ（16px）、大見出し（38px）、イラスト（高さ385px）、解説文（24px、丸型ナンバリング❶❷❸＋深緑バー）。
   - **画面左右（x: 40px / x: 1450px）**: キャラクター配置（左: ずんだもん、右: めたん）。
   - **画面下部（y: 930〜1040px）**: 字幕バー。中央カード下端から最低150px以上の余白を空け、32〜34px けいフォントで表示。

2. **「けいフォント」完全適用（Chromiumロード保証）**:
   - `src/load-font.ts` の `useEnsureKeiFont()` をコンポーネントルートで呼び出し、フォントが準備できるまで `delayRender` で待機すること。フォールバックでヒラギノ角ゴ等を表示させてはならない。

---

## 3. 台本・ストーリー構成ルール

1. **ユーザーネタの「掛け合いコント化」**:
   - 提供されたネタ（自虐・地域あるある等）は1台詞で終わらせず、ずんだもんとめたんの**3〜5往復のテンポ良い掛け合いコント**に展開すること。
   - 構成: ずんだもんの提起 ➔ めたんの勘違いツッコミ ➔ ずんだもんの真実暴露 ➔ めたんの共感 ➔ ずんだもんのオチ。
2. **話速とテンポ**:
   - VOICEVOX話速: `1.16〜1.18倍`、抑揚: `1.15〜1.20`。漢字の読み間違いは `voice_text` でふりがなを指定。

---

## 4. 必須素材・クレジット一覧

- **音声合成**: VOICEVOX（ずんだもん ID:3, 四国めたん ID:2）➔ `VOICEVOX:ずんだもん` `VOICEVOX:四国めたん`
- **立ち絵**: 坂本アヒル様（`public/characters/`）➔ `立ち絵素材: 坂本アヒル様`
- **フォント**: けいフォント（フォントな・MODI工場様）➔ `フォント: けいフォント`
- **背景**: みんちりえ様（`public/backgrounds/`）➔ `背景: みんちりえ`
- **BGM**: しゃろう様「2:23 AM」（`public/audio/bgm.mp3`）➔ `BGM: しゃろう`
- **動画フレームワーク**: Remotion (React 18 + TypeScript)
